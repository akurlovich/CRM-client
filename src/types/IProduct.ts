export interface IProduct {
  _id: string,
  title: string,
  price: number,
  dimension: string,
  count: number,
}

export interface IProductNew {
  title: string,
  price: number,
  dimension: string,
  count: number,
}